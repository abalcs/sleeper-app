{
  "name": "sleeper-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix client install && npm --prefix client run build && npm install --prefix server",
    "start": "node server/src/server.js"
  },
    "dependencies": {
        "concurrently": "^9.2.1"
    }
}
